import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFFFFE717); // Yellow
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color red = Color(0xFFF52D56);
  static const Color green = Color(0xFFA6FB89);

  // Shades of Primary Yellow
  static const Color primaryLight = Color(0xFFFFF176);
  static const Color primaryDark = Color(0xFFFFD700);

  // Background Colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color darkBackground = Color(0xFF1C1C1C);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text Colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF6F6F6F);
  static const Color textHint = Color(0xFFBDBDBD);
  static const Color textOnPrimary = Color(0xFF000000); // Black text on yellow

  // Grey Shades
  static const Color grey = Color(0xFF9E9E9E);
  static const Color greyLight = Color(0xFFE0E0E0);
  static const Color greyDark = Color(0xFF616161);

  // Shadow
  static const Color shadow = Color(0x1A000000);

  // Transparent
  static const Color transparent = Colors.transparent;
}